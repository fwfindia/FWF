{
  "nixpacks": {
    "phases": {
      "setup": {
        "nixPkgs": ["nodejs_20"]
      },
      "install": {
        "cmds": ["cd backend && npm install"]
      },
      "build": {
        "cmds": ["echo 'Build complete'"]
      }
    },
    "start": {
      "cmd": "cd backend && node server.js"
    }
  }
}
